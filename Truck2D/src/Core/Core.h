#pragma once

/*
* These typedef makes easier to work with something concepts
*/
typedef unsigned char Byte;
typedef unsigned int GPU_HANDLE;
typedef unsigned int GPU_PARAMETER;


#define EXPORT __declspec(dllexport)
#define FORCEINLINE __forceinline